# Music

Drop optional background music here.

Examples:

```text
theme.mp3
background.m4a
soundtrack.wav
```

The easy command will find the first audio file in this folder:

```bash
python3 -m codex_video make
```

The tool keeps narration clear by normalizing voice, ducking music under speech, and limiting the final mix.
